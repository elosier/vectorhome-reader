{
  "name": "vectorhome-reader",
  "version": "1.0.0",
  "description": "A self-hosted RSS reader (Google Reader / Feedly substitute) with feed discovery, scraping fallback, categories, read/unread tracking, and OPML import/export.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node --disable-warning=ExperimentalWarning server.js",
    "dev": "node --disable-warning=ExperimentalWarning --watch server.js",
    "test": "node --disable-warning=ExperimentalWarning --test"
  },
  "engines": {
    "node": ">=22.5"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "express": "^4.21.2",
    "rss-parser": "^3.13.0"
  }
}
